<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_PATH" value="logs" />
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!-- 添加过滤器，过滤掉低于INFO级别的日志 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出 - 所有日志 -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/kafka-ws.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/history/kafka-ws.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出 - 错误日志 -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/kafka-ws-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/history/kafka-ws-error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 异步输出 -->
    <appender name="ASYNC_FILE_ALL" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>1024</queueSize>
        <appender-ref ref="FILE_ALL" />
    </appender>

    <appender name="ASYNC_FILE_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>512</queueSize>
        <appender-ref ref="FILE_ERROR" />
    </appender>

    <!-- 项目包的日志配置 -->
    <logger name="org.shiguang.ws" level="DEBUG" />
    <logger name="org.springframework.web" level="INFO" />
    <logger name="org.springframework.messaging" level="INFO" />
    <logger name="org.springframework.web.socket" level="INFO" />
    
    <!-- Spark相关日志配置，设置为ERROR级别减少输出 -->
    <logger name="org.apache.spark" level="ERROR" />
    <logger name="org.apache.spark.streaming" level="ERROR" />
    <logger name="org.apache.spark.scheduler" level="ERROR" />
    <logger name="org.apache.spark.storage" level="ERROR" />
    <logger name="org.apache.spark.executor" level="ERROR" />
    <logger name="org.apache.spark.sql" level="ERROR" />
    
    <!-- Kafka相关日志配置，设置为ERROR级别减少输出 -->
    <logger name="org.apache.kafka" level="ERROR" />
    <logger name="kafka" level="ERROR" />
    
    <!-- 设置传感器和股票数据处理服务的日志级别为INFO，只输出关键信息 -->
    <logger name="org.shiguang.module.sensor.service" level="INFO" />
    <logger name="org.shiguang.module.stock.service" level="INFO" />
    <logger name="org.shiguang.module.stock.service.StockDataProcessingService" level="INFO" />
    
    <!-- 过滤掉特定的警告消息 -->
    <logger name="org.apache.spark.sql.execution.window.WindowExec" level="ERROR" />
    <logger name="org.apache.spark.sql.execution.aggregate.HashAggregateExec" level="ERROR" />
    <logger name="org.apache.spark.sql.catalyst.expressions.codegen" level="ERROR" />
    
    <!-- 其他不必要的系统日志 -->
    <logger name="org.apache.hadoop" level="ERROR" />
    <logger name="io.netty" level="ERROR" />
    <logger name="org.apache.zookeeper" level="ERROR" />

    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ASYNC_FILE_ALL" />
        <appender-ref ref="ASYNC_FILE_ERROR" />
    </root>
</configuration> 